import 'package:flutter/material.dart';

class ItemConnector extends StatelessWidget {
  const ItemConnector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
